Hi <%= @entry.name %>,

<%= @from %> posted you a message through <%= Settings.platform_name %>:

<%= @text %>

<% if @would_drive %>
--------------------------
<%= @from %> has offered to drive the car if needed.
<% end %>

--------------------------

Your entry can be seen under the following link:

  <%= entry_url(@entry) %>

If your entry should be removed from our database because it is outdated or no longer neccessary, please click on the following link.

<%= entry_remove_url(@entry, secret: @entry.secret, locale: I18n.locale) %>
